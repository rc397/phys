__pycache__/
*.pyc
.claude/
.DS_Store

# raw footage stays out, every clip is far past github's 100 MB file limit
Videos/
*.MOV

# per-video calibration caches
*.volare.json
*.volare_masks.npz
*.volare_motion.npz
*.volare_flux.npz

# other groups' data, kept local only
miachen*
output/miachen*

# local working notes
REPORT_NOTES.md
